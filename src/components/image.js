
// Gatsby-image disabled due to sharp dependency issues
// Using simple img tag instead

const Image = () => {
  return (
    <div style={{ maxWidth: '300px' }}>
      <p>Image component temporarily disabled (sharp dependency issue)</p>
    </div>
  )
}

export default Image
